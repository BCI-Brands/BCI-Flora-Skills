{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *skills/flora-batch/scripts/*.py) python3 -m pytest skills/flora-batch/scripts/tests -q ;; esac; }",
            "statusMessage": "Running flora-batch tests..."
          }
        ]
      }
    ]
  }
}
