[tool.ruff]
target-version = "py39"
include = ["skills/flora-batch/scripts/**/*.py"]

[tool.ruff.lint]
# Existing scripts intentionally use compact one-liners and combined imports;
# only flag real bugs (unused/undefined names, etc.), not style.
select = ["F"]
